{
  "name": "fetch-auth-manager",
  "version": "0.2.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "apollo-link": "^1.2.13",
    "apollo-utilities": "^1.3.2",
    "jsonwebtoken": "^8.5.1",
    "secure-random-string": "^1.1.2"
  },
  "scripts": {
    "build:client": "babel src/index.js --out-file index.js --source-maps",
    "build:server": "babel src/server.js --out-file server.js --source-maps",
    "build": "yarn build:client && yarn build:server",
    "prepublish": "yarn build"
  },
  "browserslist": "> 0.25%, not dead",
  "devDependencies": {
    "@babel/cli": "^7.6.0",
    "@babel/core": "^7.6.0",
    "@babel/preset-env": "^7.6.0",
    "eslint": "^5.16.0",
    "eslint-config-airbnb-base": "^14.0.0",
    "eslint-plugin-import": "^2.18.2"
  }
}
